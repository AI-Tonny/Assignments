Спочатку створив проект через 'django-admin startproject core .(Поточна папка де створювати, перед цим ввійшов в папку
через cd Django_Intro)' потім постворював apps (shoes, recipes 'python manage.py startapp shoes/recipes') написав для
кожного models.py пододавав для них enums.py (в мене тут є запитання, я розумію якщо поле не обов'язкове треба писати
null=True, а якщо обов'язкове писати null=False не треба (тому, що воно обов'язкове за замовчуванням)? Чи можна
прописати, явно?), потім написав forms.py це для отримання даних через форми, потім через Claude AI постворював
web(перед цим проглянувши їх та внесучи в них правки), потім написав для кожного views.py вони схожі просто модельки
різні, потім подобавляв urls.py прив'язав url шляхи до певних методів і підєднав до 'основго рута', подобавляв apps до
'core.settings INSTALLED_APPS' і запустив app 'python manage.py runserver', через мою структуру я спочатку писав 'cd
Django_Intro'.
Складнощів в принципі не було, я зрозумів, що краще мати код окремо від інших не в окремій папці, а в окремому проекті
або гілці, тому що не треба створювати додаткову папку 'Django_Intro', легше писати команди зв'язані з цим кодом і
робити імпорти.