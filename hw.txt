Опис завдання:
## 📝 Домашнє завдання для студентів
1. **News Schema:** Реалізувати `Query` для додатку `News`. Додати можливість отримувати новини разом з ім'ям автора.
2. **Filtering:** Додати аргумент `search` до запиту `all_products`, щоб можна було фільтрувати товари за назвою
(наприклад: `allProducts(search: "Phone")`). - *Підказка:* `if search: return Product.objects.filter(name__icontains=search)`.
3. **Update Mutation:** Реалізувати мутацію `updateProduct`, яка приймає ID товару і нові дані. Перевірити, чи
користувач є власником товару перед зміною (логіка, аналогічна `IsOwnerOrReadOnly`).